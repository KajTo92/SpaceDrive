import { supabase } from "./supabase-client.js";
const form=document.querySelector("[data-register-form]"),message=document.querySelector("[data-register-message]");
form?.addEventListener("submit",async event=>{event.preventDefault();const values=Object.fromEntries(new FormData(form));if(values.password!==values.confirmPassword){message.textContent="Passwords do not match.";return;}message.textContent="Creating your account…";const driver=form.dataset.registerForm==="driver";const metadata={first_name:values.firstName,last_name:values.lastName,phone:values.phone};if(driver){metadata.driver_application_requested=true;metadata.languages=values.languages.split(",").map(x=>x.trim()).filter(Boolean);metadata.application_note=values.applicationNote||"";}const{data,error}=await supabase.auth.signUp({email:values.email,password:values.password,options:{data:metadata,emailRedirectTo:`${location.origin}/login.html`}});if(error){message.textContent=error.message;return;}form.hidden=true;message.textContent=data.session?(driver?"Driver application received. Your account is waiting for approval.":"Account created. You can open the Passenger Portal."):driver?"Driver application received. Check your email, then wait for admin approval.":"Check your email to confirm your account.";});
