// For this static build, set the public Mapbox token here at deploy time.
// A future Next.js migration can map NEXT_PUBLIC_MAPBOX_TOKEN to this value.
globalThis.SPACE_DRIVE_CONFIG = globalThis.SPACE_DRIVE_CONFIG || {
  NEXT_PUBLIC_MAPBOX_TOKEN: "",
};
